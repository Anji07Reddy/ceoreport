# SQLite database for the CRM report dashboard.
# Swap to a Postgres URL (postgresql://...) and change the provider in
# prisma/schema.prisma to "postgresql" if you prefer Postgres.
DATABASE_URL="file:./dev.db"
